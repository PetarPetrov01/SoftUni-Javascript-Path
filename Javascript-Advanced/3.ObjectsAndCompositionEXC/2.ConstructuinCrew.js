function constructionCrew(workerObj) {
    if (workerObj.dizziness){
        levelOfHydrated = 0.1*workerObj.weight*workerObj.experience
        dizziness = false
    }

        return workerObj
}
constructionCrew({
    weight: 80,
    experience: 1,
    levelOfHydrated: 0,
    dizziness: true
}
)